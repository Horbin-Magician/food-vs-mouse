# 分步实施验证记录

状态：持续更新；未列为通过的验收均未完成。日期：2026-09-13。环境：macOS、Apple M4、Godot 4.6.3 stable、Compatibility OpenGL 4.1 Metal。

## 步骤 1：战斗骨架

- 建立主场景、资源定义、独立运行状态、阵地／战斗／波次／流程模块，初始三卡与第一关。
- `--headless --editor --import`：脚本与资源无解析错误。本机沙箱无法保存全局编辑器配置；系统证书读取产生环境错误。后续运行指定 `/tmp` 日志避免 Godot 在沙箱无法创建 user 日志时崩溃。
- `--headless --log-file /tmp/fvm-run.log --quit-after 120`：退出 0，无脚本错误。
- `--headless --log-file /tmp/fvm-test.log --script tests/test_core.gd`：8 项通过，含放置重复／冷却／费用、暂停、弹体击杀、粮仓归零优先。
- 真实游戏窗口：确认中文显示、5×8 阵地、卡牌、开战、放置、热量扣除、暂停反馈和入口提示。当前为原创程序绘制占位视觉。
- 完整第一关人工游玩、缩放、取消和失败后重开尚待最终视觉回归；不能据此宣称第一里程碑全部验收通过。

## 步骤 2：持续阵地

- 接入三关流程、跑腿鼠与锅盖鼠、关后奖励、生命恢复、调位和一次性维修。
- `tests/test_progression.gd`：三关状态转换、15% 恢复、奖励不可重复、调位保留生命、维修费用与次数限制、开战清除计时通过。测试直接触发清关检查流程，不代表自然战斗通关或平衡通过。
- 第二、三关实际游玩与调位视觉验收留待完整版本回归。

## 步骤 3：肉鸽闭环

- 商店三商品、限购、刷新限制、满星退出、升星同步，食谱候选／空池回退／幂等选择，八种美食与十二种食谱资源和计算已接入。
- `test_builds.gd` 通过：相同种子商店、购买重复／余额、保留损失生命、范围／星级／早餐乘算、护甲不被持续伤害消耗、候选耗尽可继续。
- `test_core.gd`、更新后的 `test_progression.gd` 与 60 帧主场景冒烟通过。无新增脚本解析错误；仍有 macOS 系统证书环境警告。
- 两种构筑的自然通关、所有食谱表现和面板可用性待完整内容验收；此步不宣称构筑平衡通过。

## 步骤 4：内容齐备

- 八关、六种普通鼠、精英锅盖鼠、鼠大厨与召唤物接入；首领固定数值、半血一次增援、周期召唤，面粉减速攻击、鼓手不叠加光环。
- `test_content.gd` 通过：内容数量、各关前三行不同、连续行限制、首领独立数值、半血只触发一次、召唤物阻止提前胜利、面粉／鼓手效果。
- 这是逻辑与内容覆盖，不代表无作弊可通八关；自然战斗、目标时长和两种构筑通关仍需试玩。

## 步骤 5：使用体验与桌面导出

- 准备快照、独立局外解锁、逐局记录、版本校验／损坏隔离、随机状态恢复已实现；JSON 整数归一化修复后存档往返通过。
- 三条可跳过引导、卡牌和单位信息、射程、暂停限制、取消、重开／铲除确认、原创图标及基础合成音效、仅调试可用的 F3 面板已接入。
- 真实窗口发现并修复点击读取缓存鼠标位置的问题，改用事件坐标；`test_ui.gd` 保留回归。实际验证：购买扣 3 金与售罄、开战、指定格子放置、热量扣费、暂停后拒绝放置且不扣费、Esc 取消、恢复计时、重启显示准备快照提示。
- `python3 tools/run_tests.py`：core、progression、builds、content、save、ui 六个测试脚本通过。测试超时 30 秒视为失败，避免 assert 中止脚本却不退出引擎被误报通过。
- macOS universal 官方模板导出成功，发行二进制 120 帧 headless 启动退出 0，确认包内资源目录使用 ResourceLoader 列举可正常加载；实际发行窗口最终回归见后续步骤。
- 暂停/恢复与基本界面通过；全部关间操作的真人视觉验收、音效听感、其他机器系统启动流程尚未完成。

## 步骤 6：综合回归与交付检查

- 七组回归脚本全部通过，新增 `test_edges.gd` 覆盖满盘、五布丁上限、交换对象身份与生命、首次生产等待、三目标穿透、同路同时死亡、三次灼烧跳伤不耗护甲、减速暂停及 2×时间、100 个种子的第八关生成复现。
- 在源工程真实窗口自然完成首关并进入第二关选谱，漏粮真实扣减；在独立关间夹具上验证发行应用选谱、受伤单位调位、4 金维修与重复维修拒绝（金币未重复扣除）。窗口正确显示中文和原创图标；resize 尝试因窗口控制连接丢失未完成，因此不把非默认尺寸列为实测通过。
- 最终版压力复测和两策略结果见 [平衡记录](balance.md)。优化只缓存绘制样式与鼓手来源，不修改平衡参数。平均超过 60 FPS，但 P99 仍有波动，稳定帧耗时验收保留。
- 自动化与视觉存档目录分离；新增验收夹具、自动策略及原生压力场景均排除在发行包之外。
- 尚未完成：至少 5 名新玩家／15 局、常规 20–25 分钟体验、经济和中后期决策迭代、严格稳定 60 FPS、非默认窗口尺寸与其他设备／平台测试、完整八关人工视觉体验。不得用自动策略替代真人记录。

### TODO 必验项映射

| 必验项 | 实际证据 | 限制 |
|---|---|---|
| 放置费用、冷却、重复点击 | core、ui、edges；真实放置／暂停操作 | 通过所列场景 |
| 满盘、空路、死亡和粮仓优先判负 | core、edges；真实首关漏粮 | 同步步骤结算 |
| 星级与食谱数值、不重复注册 | builds、content | 全食谱主观表现仍待试玩 |
| 维修、移动、移除、升星不复制或满血 | progression、builds、edges；发行窗口调位／维修 | 铲除对话框完整人工回归待补 |
| 暂停与倍速影响生成／生产／状态 | core、edges、ui；真实暂停 | 通过所列场景 |
| 商店原子支付、满星、食谱空池 | builds；真实购买售罄 | 通过所列场景 |
| 种子与操作复现 | builds、edges、save | 同 Godot 版本，不保证跨平台逐帧一致 |
| 存档恢复与奖励防重复 | save、progression；发行恢复夹具 | 只支持版本 1 |
| 40 美食／100 敌人性能 | 原生压力场景 | 平均通过，尾部波动待继续观察 |

## 美术资源补全

2026-09-13：四张图集／背景包含 29 项美术内容，已接入主场景；Godot 导入、七组既有回归与真实窗口验收见 [美术记录](art_pack.md)。

## 台面透视排布

2026-09-13：近宽远窄的台面、深度缩放、分行遮挡与投影命中已接入；八组测试与远近格放置／调位窗口验收见 [透视设计与记录](../art/perspective.md)。
