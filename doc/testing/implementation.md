# 分步实施验证记录

状态：持续更新；未列为通过的验收均未完成。日期：2026-09-13。环境：macOS、Apple M4、Godot 4.6.3 stable、Compatibility OpenGL 4.1 Metal。

## 步骤 1：战斗骨架

- 建立主场景、资源定义、独立运行状态、阵地／战斗／波次／流程模块，初始三卡与第一关。
- `--headless --editor --import`：脚本与资源无解析错误。本机沙箱无法保存全局编辑器配置；系统证书读取产生环境错误。后续运行指定 `/tmp` 日志避免 Godot 在沙箱无法创建 user 日志时崩溃。
- `--headless --log-file /tmp/fvm-run.log --quit-after 120`：退出 0，无脚本错误。
- `--headless --log-file /tmp/fvm-test.log --script tests/test_core.gd`：8 项通过，含放置重复／冷却／费用、暂停、弹体击杀、粮仓归零优先。
- 真实游戏窗口：确认中文显示、5×8 阵地、卡牌、开战、放置、热量扣除、暂停反馈和入口提示。当前为原创程序绘制占位视觉。
- 完整第一关人工游玩、缩放、取消和失败后重开尚待最终视觉回归；不能据此宣称第一里程碑全部验收通过。

## 步骤 2：持续阵地

- 接入三关流程、跑腿鼠与锅盖鼠、关后奖励、生命恢复、调位和一次性维修。
- `tests/test_progression.gd`：三关状态转换、15% 恢复、奖励不可重复、调位保留生命、维修费用与次数限制、开战清除计时通过。测试直接触发清关检查流程，不代表自然战斗通关或平衡通过。
- 第二、三关实际游玩与调位视觉验收留待完整版本回归。

## 步骤 3：肉鸽闭环

- 商店三商品、限购、刷新限制、满星退出、升星同步，食谱候选／空池回退／幂等选择，八种美食与十二种食谱资源和计算已接入。
- `test_builds.gd` 通过：相同种子商店、购买重复／余额、保留损失生命、范围／星级／早餐乘算、护甲不被持续伤害消耗、候选耗尽可继续。
- `test_core.gd`、更新后的 `test_progression.gd` 与 60 帧主场景冒烟通过。无新增脚本解析错误；仍有 macOS 系统证书环境警告。
- 两种构筑的自然通关、所有食谱表现和面板可用性待完整内容验收；此步不宣称构筑平衡通过。

## 步骤 4：内容齐备

- 八关、六种普通鼠、精英锅盖鼠、鼠大厨与召唤物接入；首领固定数值、半血一次增援、周期召唤，面粉减速攻击、鼓手不叠加光环。
- `test_content.gd` 通过：内容数量、各关前三行不同、连续行限制、首领独立数值、半血只触发一次、召唤物阻止提前胜利、面粉／鼓手效果。
- 这是逻辑与内容覆盖，不代表无作弊可通八关；自然战斗、目标时长和两种构筑通关仍需试玩。
