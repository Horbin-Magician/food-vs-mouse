# 正交布局验证

状态：已实现，自动及渲染图检查完成；真实鼠标复验待完成。范围：2026-09-13 工作区布局更新，保留已有单位动画改动。设计见 [棋盘布局](../art/perspective.md)。

环境：macOS、Apple M4、Godot 4.6.3 stable、Compatibility，逻辑分辨率 1280×720。测试使用隔离的 `--qa-test` 存档。

- Godot 编辑器无界面导入完成，无新增解析错误或丢失资源引用。
- `python3 tools/run_tests.py`：九组回归通过，包含动画、内容、构筑、核心逻辑、边界、关间流程、坐标映射、存档与 UI。
- 坐标测试覆盖全部 40 格的 200 个内部采样点、外侧拒绝、水平／垂直网格及各行同尺寸。UI 回归覆盖重复放置、暂停、倍速动画、锅铲确认与取消、八张卡边界、浮层阻止点击穿透及收起后末列可操作。
- 使用真实 OpenGL 渲染器加载 `tests/art_gallery.tscn`，导出 Viewport 图检查完整八卡、十六单位及展开／收起商店。首次发现卡牌默认图标被压缩及冷却文字重叠，改为独立图标与文字后复查通过。当前卡栏、美食费用、冷却、锅铲图标和右下角关卡信息完整可见，棋盘与背景平行对齐。
- 原生 UI 工具始终选中 Godot 编辑器，未能可靠操作独立游戏窗口；因此上述渲染检查和事件回归不作为真实鼠标验收。真实鼠标放置／调位／暂停／取消／铲除、窗口缩放、完整八关及发行包导出尚未复验。本次不宣称使用体验里程碑完全通过。
- 渲染检查脚本和图像留在 `/tmp`，不纳入仓库；未改动战斗数值及存档格式，未新增外部资产或运行依赖。

## 七行九列方格扩展验证（2026-09-13）

本节为最新工作区规则，前文 40 格／200 点记录属于扩展前布局。当前棋盘为 63 格，每格 62×62 像素，逻辑每格 96×96；行列范围、生成、召唤、弹体清理及存档校验同步扩大。

- Godot 4.6.3 导入及九组测试再次通过，`git diff --check` 通过。
- projection：63 格共 315 个内部采样点、外侧拒绝、正交方向与 62×62 等宽高断言通过。
- edges/save/UI：63 格满盘及保存恢复一致、仍限定五个布丁、越界行列拒绝、末行末列点击、暂停与锅铲确认等回归通过。旧版 5×8 范围内的坐标仍合法，保存格式不变。
- content：首关行索引 2、3、4，后续 0–6，首领中央行 3；固定种子生成回归通过。
- 实际 OpenGL Viewport 渲染检查：完整 7×9 等大方格、缩放后单位、顶部八卡、锅铲、右下角关卡及小铺展开／收起均可见。当前小铺位于棋盘右侧，不覆盖格子内部。
- 未完成真实鼠标／窗口缩放复验、63 单位压力测量或七路全局平衡试玩；旧版五路自动通关与性能记录不代表扩展后已验收。未导出发行包。

## 底部 UI 精简（2026-09-13）

- 移除底部左侧面板及其常驻文本，右侧矩形信息框替换为分段进度条、终点旗标与单行状态。操作与悬停反馈保留在顶部提示行。
- Godot 4.6.3 导入与九组回归通过；新增进度状态断言后单独重跑 UI 通过，覆盖空生成表、50%、满条仍清怪、暂停、下一关归零与胜利满条。该单独 UI 运行退出时有 ObjectDB 泄漏警告，未出现脚本错误；完整实例清理仍待排查，不将此项记为无警告通过。
- 真实 OpenGL 渲染检查准备空条与暂停 50% 两种状态：左下留空、单行文字和细条完整可见，未遮挡棋盘。临时截图夹具首次使用缺少 time 字段的事件导致渲染错误，修正为有效事件后重跑无错误；该夹具未进入仓库。
- 本次不改业务进度、胜败条件或时间推进。真实鼠标／缩放验证沿用前述待验收限制；截图与临时夹具不提交仓库。
