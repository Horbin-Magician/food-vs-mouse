# 美术资源包验收

状态：所列验收已完成；完整真人体验待验证。日期：2026-09-13。
环境：macOS、Apple M4、Godot 4.6.3 stable、Compatibility OpenGL 4.1 Metal。工作区新增美术版本，未提交。用户原有 project.godot 改动保留。

## 已执行

- 四张源 PNG 逐张检查主题与图集顺序；三张角色／食谱图集有真实 alpha。实际游戏中无烘焙背景方块，全部十六个角色显示正常，图集切片未出现明显相邻角色串图。
- `Godot --headless --path . --editor --import --quit`：最终导入成功，ArtCatalog、主场景与验收夹具无解析错误，PNG 引用有效。
- `python3 tools/run_tests.py`：builds、content、core、edges、progression、save、ui 七组均 PASS。保留费用、暂停、输入、星级、存档与波次等原有逻辑回归。
- 原生游戏窗口：初始三卡、小铺商品头像、厨房背景、格子与中文可见；独立 `tests/art_gallery.tscn` 显示八种美食与八种鼠群、减速和灼烧标志、精英／首领尺寸差别、全部八张持有卡，无底栏溢出。
- 按 R 切换食谱三选一，确认蒸笼／冰块／早餐三种图标与对应中文名显示；P 返回小铺。
- 真实鼠标操作：开战、选择冰柠茶并放置到第五行首格、暂停后点击空格；显示「仅可在未暂停的战斗中放置」，热量保持 29、无新增角色；Esc 清空选择。暂停画面角色与冷却停止推进。
- 窗口观察覆盖较小窗口和放大后的窗口；后者保持比例并出现黑边，八卡完整可见。未做所有桌面 DPI／多显示器组合测试。
- 图集区域由 AtlasTexture 缓存，源 PNG、导入配置及新 GDScript UID 保留；发行预设已有 tests/* 排除规则。

## 后续边缘缺陷修复

初次检查未发现的跨格碎片、断尾和截帽问题已通过重新分离、排版图集修复，详细证据与当前验证见 [图集边缘修整](asset_edges.md)。初次目视结果不代表像素级裁切正确。

## 限制

本次不重新导出发行包，不代表发行包已更新。未进行完整八关真人视觉回归、所有食谱逐个鼠标选择、全部攻击动作动画或多设备测试。角色为单姿势图集与程序浮动，仍无音乐；原有真人和平衡验收缺口不变。

## 压力复测

执行 `Godot --path . tests/stress.tscn -- --qa-test`（原生窗口，未使用 headless）。固定 40 美食、100 敌人及攻击，预热 3 秒后采样 20.040 秒／2400 帧：平均 119.76 FPS，P95 10.61 ms，P99 16.67 ms，Apple M4、逻辑 1280×720。本次平均帧率通过 60 FPS 目标；短时采样不能证明全局稳定 60 FPS，不替代原有性能／真人验收。日志留在 /tmp，未入库。
