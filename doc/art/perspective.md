# 厨房台面透视排布

状态：已接入，以下自动及窗口验收已完成。范围：首版使用体验，仅表现及鼠标坐标映射。沿用 [玩法基线](../TODO.md) 的五行八列和逻辑格宽 96。

## 设计

战场以背景台面为依据：背景适配 1280×720 后，台面后沿约为 (231,151)→(1086,151)，前沿约为 (100,600)→(1202,600)。两条横沿保持水平；纵向边缘延长后汇聚于同一消失点。战场取该台面的局部区域，横向范围 3.5%～73.3%，画布纵向范围 y=190～516，右侧给操作面板留空间。不能单独旋转棋盘或给右侧额外抬高。

`BoardProjection` 从台面边缘插值计算战场角点（约 (250,190)、(862,190)、(898,516)、(161,516)），使用射影深度比例分配行距，纵向格线与台面侧边共享消失点。格子采用低透明度台面色与细线接缝，悬停与选中格显示清晰描边。UI 面板保持平面排版。

角色保持直立，以投影后的脚底为锚，随行深度由小到大；影子贴在台面，后排先绘制、前排后绘制。同一行的食物和老鼠共享深度。血条和状态跟随角色；弹体、入口提示、粮仓出口、射程区域使用同一投影。视觉变化不改动逻辑距离、速度、行列、存档或伤害，不引入随机偏移。

`BoardProjection` 集中管理逻辑坐标到画布的映射、40 个投影格子与命中检测。输入与悬停都命中四边形，不沿用旧矩形包围盒；外侧点击无效，内部公共边界按固定遍历顺序归属，不重复操作。角色上部可能跨越格线，点击仍以脚下格子为准。

## 验收

自动检查全部 40 格中心、靠近边界的内点、四角和台面外侧命中；真实鼠标验证远／近行放置、准备调位、悬停射程、暂停与取消。Godot 导入及 UI 回归通过后检查真实窗口的轮廓、深度排序、弹体同路与面板边界。性能、发行包及完整八关体验未验证则明确记录。


## 实际验证（2026-09-13）

环境：macOS、Apple M4、Godot 4.6.3 stable、Compatibility。未新增依赖或图片，不改写用户既有工程配置。

- Godot 无界面导入通过，无新增解析或引用错误。
- `python3 tools/run_tests.py` 八组 PASS；新增 projection 覆盖 40 格共 200 个内部采样点（中心与近边缘）、已知角点、六个台面外侧点、逐行连续路径。UI 回归改用投影坐标，重复点击和暂停不放置通过。
- 原生 `tests/art_gallery.tscn` 窗口：全部角色随台面排布，格线与厨房纹理融合（首次版本，后续对齐修正见下文）；首领、精英、状态、生命条显示正常，面板保持平直，UI 底板最后绘制以遮挡进入面板区域的场景对象。
- 真实鼠标将最远排小笼包移到最近排，再移至中央行首格；生命保持 180/180，角色近大远小，悬停射程沿投影格子显示。
- 开战后放置远排小笼包与近排布丁；选错冰柠茶时正常提示热量不足，未错误放置。暂停点击空格提示「仅可在未暂停的战斗中放置」，热量保持 13；Esc 清除选中。
- 未重新导出发行包，未完成完整八关真人体验或多 DPI 组合回归。投影为适合 2D 贴图的风格化台面映射，不引入 3D 摄像机或改变逻辑战斗坐标。

压力复测：原生 `tests/stress.tscn -- --qa-test`，40 美食＋100 敌人及攻击；预热后采样 20.026 秒／2503 帧，平均 124.99 FPS，P95 9.52 ms、P99 14.35 ms，Apple M4、逻辑 1280×720。短时平均达到 60 FPS 目标，不推断其他设备或整局稳定表现；日志留在 /tmp。


## 场景对齐修正（2026-09-13）

用户指出首次版本的独立倾斜与背景不一致。已废弃右上抬高的四角配置，按上述背景边缘重新计算投影。此次 Godot 导入与八组自动测试通过；增加每条横线等高、九条纵线与背景台面共享消失点的断言。真实窗口检查棋盘横沿与台面横沿平行，并完成远排到近排的鼠标调位，生命保持 180/180。前文压力数据属于首次版本，此次未重复性能测试或导出安装包。
