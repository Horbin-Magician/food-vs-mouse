# 热量拾取验证

状态：所列逻辑、输入及窗口拾取检查通过，完整平衡和窗口缩放复验待完成。设计见 [热量生产与拾取](../design/heat.md)。

## 环境与范围

2026-09-13，macOS／Apple M4、Godot 4.6.3.stable.official.7d41c59c4、Compatibility／OpenGL 4.1 Metal。基于含既有顶部 UI、锅铲、卡牌拖放改动的未提交工作区；不覆盖这些修改。本项对应首版战斗骨架／使用体验及经济参数迭代，不代表里程碑整体通过。

## 自动验证

- `Godot --headless --path . --editor --import --quit`：最终导入退出 0，无新增脚本解析错误或缺失引用；新脚本 UID 已生成。
- `python3 tools/run_tests.py`：12 组全部通过（animation、builds、content、core、drag、edges、heat、heat_ui、progression、projection、save、ui）。
- `test_heat.gd`：准备不增长、自然 2／秒、首次完整周期才生成、生产不入账、多周期合并、二星＋焦糖加倍快照、点击后延迟入账、重复及失效 uid、暂停冻结待收和飞行、2×、飞行中再生产、满热量生产及到达溢出、来源移除／死亡、关末／最终胜利／失败／重开／准备快照恢复清理。
- `test_heat_ui.gd`：经 Viewport 投递真实 GUI 事件，覆盖选卡／锅铲下拾取不穿透、暂停拒绝、模态遮挡、重复点击、飞行朝真实数值控件中心移动、到达后数值显示更新及动态规则说明。
- 更新 edges 的直接产热旧断言；autoplay 改为调用正常拾取接口并等待飞行入账。此次未重新运行两策略八关平衡，不沿用旧热量经济的通关结论。

## 原生渲染与鼠标检查

使用 `tests/heat_gallery.tscn -- --qa`，放置首行左端、中央及末行右端三份布丁；敌人事件延后，首周期从剩余 0.5 秒开始，后续维持真实 10 秒生产和正常收取。不写玩家正式存档。因工具最初连接到既有 Godot 游戏窗口，改用 `/tmp` 中的本机 Godot 二进制独立 QA 应用完成检查，不修改项目引擎、依赖或发行配置。

- 实際窗口可见三团橙金火苗、光晕及 +15／+45／+60 合并数量，火苗处于来源格上方；检查初始窗口及放大窗口，未挤占顶部资源显示。
- 点击暂停后点击中央火苗，仍保留 +60，显示“暂停时不能收取火苗”，热量停在 167；恢复后点击并再次暂停，中央火苗消失，热量到 230（60 拾取＋这段时间的自然恢复）。
- 原生 GPU 中途截图 `/tmp/food_heat_flight.png` 可见中央火苗已离开来源、沿左上方向飞行，余额仍为 169，尚未加入这笔 60；到达后才增加数值。截图为本次临时证据，不纳入仓库。
- 选中包子后拾取首行火苗，激活锅铲后拾取末行火苗；三份布丁仍存活，没有额外放置、没有铲除确认，余额封顶 350。
- 2×按钮可切换，精确生产／飞行加速以自动测试为证据。原生窗口从放大恢复到小尺寸、再手动调整大小时，工具画面出现裁切／灰色未更新区，未将这一过程记为完整缩放通过；无脚本错误日志。此限制需要独立复验，不扩大为本次热量改动的已确认缺陷。

## 剩余事项

- 自然恢复由 5 降到 2 后的首关及完整八关真人难度、经营收益和两种构筑通关需重测；旧自动通关与性能记录是历史数据。
- 未执行完整压力测试，未重导发行包；发行包暂不包含本次源工程修改。
- 非默认窗口尺寸下完整鼠标命中、暂停与重复点击复验尚未全部完成。
