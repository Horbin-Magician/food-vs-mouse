# 单位动画

状态：已实现；实际验证与剩余验收见文末。对应使用体验里程碑，覆盖 8 美食及 8 鼠群（含精英与首领）。

使用现有原创单姿势图集进行脚底锚定的位移、旋转和挤压伸展，不新增依赖或资产许可。不是逐帧肢体动画；不改变伤害、碰撞、攻击周期、格子占用或存档格式。

- 所有单位待机轻微呼吸，各 ID 有独立幅度与节奏，实例相位错开，不消耗玩法随机数。
- 成功放置美食／生成老鼠播放 0.38 秒落地回弹；失败操作无动画。恢复存档不重播放置。
- 六种攻击美食在实际出手时后坐或前冲；布丁产热时弹跳，吐司受击时防御挤压，不凭空增加攻击。老鼠在实际造成近战伤害时向左啃咬，鼓手和首领采用更重摆幅。
- 老鼠仅在未被阻挡时走路，步幅按实际移动距离推进，所以减速、鼓舞和狂暴会影响步频。阻挡后切回待机／攻击。
- 准备调位与交换立即提交逻辑位置，双方在 0.32 秒内沿弧线移到目标；连续调位从当前显示位置接续，同格操作无动画。美食战斗时不自行行走。
- 优先级：调位／放置叠加出手，出手覆盖待机姿态，受击闪白独立叠加。影子留在地面，血条随显示脚点移动，鼠标命中仍以目标逻辑格为准。
- 表现状态由 UnitAnimator 独立持有。BoardController 发 placed(unit)、moved(unit, from_row, from_col)，CombatController 发 acted(uid)，仅成功业务事件发出。移动通过实际 x 差识别。退出战斗清除动作，移除实体清理缓存，重开／读档更换状态时重建绑定。
- 战斗使用 RunState.elapsed 的实际增量；暂停冻结全部表现。准备使用现实 delta（不受战斗倍速影响），选谱／胜败冻结。时间不写入共享 Resource 或存档。

验收：全部 ID 覆盖、出手同步、交换与连续移动、移除清理、暂停／2×、导入及现有逻辑回归；原生窗口观察待机、放置、攻击、行走与准备调位。不得用自动测试代替视觉检查。

## 实际验证（2026-09-13）

状态：代码与原生渲染检查完成，真实鼠标交互复验待完成。环境：macOS、Apple M4、Godot 4.6.3 stable、Compatibility、1280×720。本次未改变玩法配置、随机源或存档格式。

- Godot `--headless --editor --import --quit` 通过，无新增解析或资源引用错误。
- `python3 tools/run_tests.py` 九组全部 PASS。新增 `test_animation.gd` 验证 16 ID、放置失败不触发、真实攻击／产热事件、移动步幅、零时间冻结、双方交换、连续调位衔接、生命不变、移除及重开清理。UI 回归增加暂停表现时间不变、2× 表现推进与实际模拟时间一致断言。
- 原生 `tests/animation_gallery.tscn -- --qa-test --capture-animation` 运行通过，逐一检查 viewport 输出的待机、入场、攻击／产热、调位／行走四个画面：角色贴图、脚点、血条正常，倾斜、跳跃与回弹位置可见。最终版本复查调位画面，影子沿地面路径。截图在 `/tmp/food_animation_0.png` 至 `/tmp/food_animation_3.png`，不入库。
- 可重复验收场景 `tests/animation_gallery.tscn` 每 2 秒切换一种表现，准备阶段支持正常鼠标调位；这是显式触发表现的测试场景，不代表自动执行了全部正常战斗交互。工具定位到 Godot 编辑器，未成功切换独立运行窗口进行真实鼠标复验。完整逐帧观看、真实鼠标放置／交换／取消、多窗口尺寸和八关体验仍待人工验收。
- 原生压力场景：40 美食、100 敌人及弹体，预热后约 20.00 秒／1896 帧，平均 94.80 FPS，P95 12.84 ms、P99 20.38 ms。短时平均超过 60 FPS；P99 超过 16.67 ms，不能宣称稳定每帧 60 FPS。测量时另有验收窗口运行，不与此前压力结果作严格性能回归比较；未重新导出发行包。
