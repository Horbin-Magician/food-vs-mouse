# 全局 UI 优化验收

## 美食卡拖放（2026-09-13）

状态：已实现，完成本项导入、回归、渲染与窗口验证。依据 [拖放设计](../art/ui.md#美食卡拖放2026-09-13)，属于「使用体验」里程碑。保留工作区已有且同期更新的紧凑顶栏、棋盘、火焰图标和锅铲光标改动。

- 环境：macOS、Apple M4、Godot 4.6.3 stable、Compatibility/OpenGL 4.1 Metal，逻辑 1280×720；使用 `--qa-test` 隔离存档。临时窗口使用本机官方 Godot 副本 `/tmp/FVMDragQA.app`，测试夹具开放八卡并以零 delta 固定战斗时钟，方便精确比较拖动前后费用；不作为正常流程／计时验收。
- 无界面编辑器导入通过，无解析或资源引用错误；`python3 tools/run_tests.py` 十组通过（新增 drag，加原有九组）。修正提示框首次显示宽度后，drag 与 UI 再次通过。退出仍可见此前记录的音频 ObjectDB 警告，无新增脚本错误。
- `tests/test_drag.gd` 通过 Viewport 输入队列发送鼠标／键盘事件，覆盖卡片 GUI 按下、移动阈值、松手单次放置、重复松手、预览不改变冷却或费用、松手重新校验热量、占用、冷却、五布丁上限、棋盘外／其他按钮松手、右键／Esc／失焦取消、暂停及阶段切换、准备拒绝、弹窗清理和点击选卡兼容。失败和取消均保持单位数及费用。
- 实际 OpenGL 渲染检查绿色合法目标、红色热量不足目标、半透明美食跟随及对应文字。发现并修正浮动提示首次出现时因未预设换行宽度造成的单帧高度膨胀；复查合法提示已为紧凑单行，非法提示亦正常。
- 原生 UI 工具驱动真实窗口：关闭小铺并开战，将小笼包从顶栏拖至第一行第一格，热量 150→50、只出现一个单位；布丁拖出棋盘取消，仍为 50。系统放大窗口后将布丁拖至第七行第九格，准确落位、热量 50→0；点击暂停再拖吐司到第七行第一格，显示仅可在未暂停战斗放置，未新增单位，热量仍为 0。
- 默认及放大窗口捕获约 640×392、1224×768（含标题栏和系统捕获缩放），卡牌与棋盘完整、方格及比例保持、边缘落点正确。自动事件检查覆盖右键／Esc、失焦及弹窗取消，本轮未逐一用原生鼠标复验这些分支。
- 设计、接口、基线及索引同步；未改变存档格式或数值，未新增外部依赖。未重导发行包，未重复完整八关、真人平衡或性能验收。临时包装、脚本、日志和截图均在 `/tmp`，不纳入仓库。


## 左侧信息卡片移除（2026-09-13）

状态：已实现，导入、既有 UI 回归和真实窗口检查通过。适用「使用体验」中的左侧信息卡片删除，依据 [UI 规范](../art/ui.md)。

- 删除卡片底板、品牌、阶段与情境说明、分隔线及分段耐久条，同时删除对应 Label 创建和状态刷新。顶部粮仓数值、右下关卡状态与棋盘行号保留。
- macOS、Apple M4、Godot 4.6.3 stable、Compatibility/OpenGL 4.1 Metal，逻辑 1280×720；无界面编辑器导入与既有 `tests/test_ui.gd -- --qa-test` 通过。UI 退出仍出现已记录的 ObjectDB 警告，没有解析或断言错误；本次未新增测试或修改业务。
- 使用 `/tmp/FVMRemoveCardQA.app` 临时独立应用标识与 `--qa-test` 隔离存档检查真实窗口：准备、开战、选布丁并放置于第三行第二格、暂停与 Esc 取消均可用，左侧没有卡片、文字或耐久残留。默认窗口及系统放大窗口（捕获约 640×392、1224×768，含标题栏）均保持方格与比例，顶部资源和右下状态可见。
- 验证时控制栏移至底部的并行改动已存在，予以保留；商店弹窗另有并行设计，本节不代表其验收。未重复完整九组业务、八关真人、性能或发行导出；临时应用和截图不入库。

## 原全局 UI 优化记录

状态：已实现，自动回归、真实渲染和窗口鼠标检查通过。范围：2026-09-13 工作区「使用体验」UI 优化，设计依据 [UI 规范](../art/ui.md)。不替代完整八关真人、平衡或发行验收。

## 环境与隔离

macOS、Apple M4、Godot 4.6.3 stable、Compatibility/OpenGL 4.1 Metal；逻辑 1280×720。自动及窗口测试使用 `--qa-test` 隔离存档，未修改玩家存档、数值资源和棋盘坐标。未增加外部资产或运行依赖。

工具默认定位 Godot 编辑器，无法直接选中独立游戏实例。此次将本机同版本 Godot 可执行文件复制到 `/tmp` 的独立 QA 应用标识并做本地临时签名，只为定位测试窗口；随后通过原生 UI 工具操作真实游戏窗口。该临时包装、截图与日志不进入仓库，不作为游戏发行包。

## 实际结果

- Godot 无界面编辑器导入通过，无新增解析或资源引用错误。
- `python3 tools/run_tests.py` 九组回归通过：animation、builds、content、core、edges、progression、projection、save、UI。覆盖原有费用、重复放置、暂停、存档、升星等必验项；玩法与业务模块保持现有规则。
- UI 增补检查通过：阶段对应的主按钮/暂停/维修禁用状态，金币不足购买禁用，准备/暂停/冷却/热量不足/选中状态，冷却条高度，八卡边界，商店及食谱面板边界，长中文说明的子控件边界，弹窗打开时禁止棋盘输入，面板与棋盘不重叠。
- 实际 OpenGL Viewport 图检查：八卡准备、小铺、三选一、暂停、确认弹窗、胜利与失败战报。发现并修正冷却条默认最小高度溢出、按钮与提示行间距不足、窗口标题背景缺失，以及长中文说明在启用自动换行前被最小宽度撑大的问题。最后一轮 UI 回归及渲染检查通过。
- 真实窗口鼠标（工具驱动）：将第一行第一格小笼包移到第七行第九格，悬停显示生命 180/180；点击开始，选择布丁并在第三行第二格放置成功；暂停后点击空格没有新增单位、热量保持 107，并出现禁止放置说明；Esc 清除选中。恢复后选锅铲、点击布丁弹出确认及暗色遮罩；取消后布丁保留；再次暂停并清除选择通过。
- 使用系统窗口缩放检查默认小窗口与放大窗口：棋盘仍为等大正方形，顶部八卡、控制栏、左侧说明与右下角状态均完整可见，保持比例并出现必要黑边。实际窗口截图约为 640×392 与 1224×768（含标题栏与系统捕获缩放），不是跨设备 DPI 保证。
- `git diff --check` 通过。新增主题脚本及 UID 为源文件；临时应用、日志、渲染图未纳入仓库。

## 限制与后续

- 部分快速无界面 UI 测试退出仍出现既有 ObjectDB 警告。`--verbose` 定位到 `AudioStreamWAV` / `AudioStreamPlaybackWAV` 的退出引用；未出现脚本或 UI 断言错误。本次不扩大到音频模块生命周期重构，不能称退出全程无警告。
- 真实鼠标未逐一复验全部商店商品、十二食谱、自然八关流程；这些流程由既有逻辑回归和代表性视觉状态检查支撑。未进行新一轮压力测试或完整真人/平衡验收。
- 未重新导出 macOS 安装包；现有发行包仍属于此前版本。


## 操作栏移至底部（2026-09-13）

状态：已实现，导入、既有 UI 回归及实际 OpenGL 渲染检查通过；本轮真实鼠标及窗口缩放复验受工具阻断。范围为「使用体验」中的操作栏位置调整，布局依据 [棋盘布局](../art/perspective.md)。

- 环境：macOS、Apple M4、Godot 4.6.3 stable、Compatibility，逻辑 1280×720，`--qa-test` 隔离存档。
- 将开始、暂停、倍速、维修、右键/Esc 提示、面板开关和重新开局移至底部；右侧关卡文字和进度条保持原位，按钮回调与业务规则未改。
- Godot 无界面编辑器导入完成；`--headless --path . --script tests/test_ui.gd -- --qa-test` 通过，覆盖放置、暂停、倍速动画、铲除确认、取消、八卡边界及面板输入。退出仍有此前记录的 ObjectDB 警告，无脚本或断言错误。
- 真实 OpenGL Viewport 检查准备、食谱、暂停、胜利和失败：六个底部按钮、取消提示与关卡信息完整可见，操作栏结束于 x=924，与 x=948 起的关卡进度保留间距，未覆盖棋盘；不同阶段主按钮文案没有溢出。截图与临时脚本存于 `/tmp`，不纳入仓库。
- 原生 UI 工具可读到独立测试窗口，但执行按键时返回 `Computer Use is not active`，重选应用及重置连接后仍不能可靠操作。本轮不将渲染或事件回归视为真实鼠标、窗口缩放验收；底部按钮鼠标点击与缩放待复验。未重导发行包。
- 验证期间工作区另有左侧信息卡片移除与商店弹窗调整，保留这些并行改动；本节只记录操作栏范围，不代替其他改动的验收。


## 小铺弹窗（2026-09-13）

状态：已实现；本轮导入、九组回归、真实窗口鼠标及缩放检查通过。范围为「使用体验」商店布局，规则依据 [小铺弹窗设计](../art/ui.md#小铺弹窗2026-09-13)，不替代完整真人、平衡或发行验收。

- 环境：macOS、Apple M4、Godot 4.6.3 stable、Compatibility/OpenGL 4.1 Metal；逻辑 1280×720。临时 QA 脚本固定种子 25、初始测试金币 10，使用 `--qa-test` 隔离存档，实例创建后禁用持久化。未修改玩家存档、数值资源或业务控制器。
- 小铺改为 840×416 的居中遮罩弹窗，三件商品等宽横排，显示图像、用途、持有进度、价格、金币、刷新及操作反馈。关闭只返回布阵；重开本局后小铺重新打开，开战时自动隐藏。
- Godot 无界面导入通过；`python3 tools/run_tests.py` 九组通过。UI 增补覆盖三卡横排与内容边界、弹窗禁止棋盘输入、关闭重开保持商品和 RNG 状态、购买后 UI 重建、重复购买不重复扣费、两次刷新扣费与禁用、零金币、全卡满星空池、关闭入口、重开与战斗阶段切换；原交易、存档、暂停与放置检查继续通过。
- 原生 UI 工具驱动真实窗口：购买布丁后金币 10→7，持有 1/6→2/6，商品变为售罄；重复点击不再扣费；两次刷新金币 7→5→3，计数 0/2→1/2→2/2 后禁用；价格 4 的辣椒串／冰柠茶明确显示金币不足且点击不购买。
- 真实鼠标检查「完成购物」、右上角关闭、Esc、右键、点击遮罩及底部再次打开；商品、金币和刷新次数保持。点击遮罩覆盖的「开始鼠潮」位置只关闭弹窗，仍为准备阶段；关闭后再点开始可进入战斗。默认与放大窗口捕获约 640×392、1224×768（含系统标题栏），弹窗居中、三卡等宽、文字及按钮无裁切，保持等比与必要黑边。
- 最初临时 UIQA 应用连接发生失效，改用本轮独立 `/tmp/FVMShopPopupQA.app` 完成关闭、重开、防穿透、缩放及开战复查。临时包装器沿用同版本官方 Godot，仅增加独立本地应用标识与签名用于窗口定位，不作为发行资源。
- `git diff --check` 与文档相对链接检查通过。既有快速测试退出的音频 ObjectDB 警告仍按前文记录；本次无新增脚本解析或断言错误。未重新导出安装包，未重复八关真人或性能验收；临时脚本、截图、日志和包装器不纳入仓库。

## 提示栏与新手引导移除（2026-09-13）

状态：已实现，导入、既有 UI 回归及实际渲染／窗口交互检查通过。范围为「使用体验」中的提示栏和步骤式引导删除，规则见 [UI 规范](../art/ui.md#提示栏与引导移除2026-09-13)。

- 删除主场景常驻消息 Label、提示圆点、引导 Label、跳过按钮与全部三步推进／重开重置逻辑；美术验收场景同步移除旧引导状态赋值。悬停单位详情和非法操作使用不拦截输入的临时浮动 Label，小铺反馈继续保留。
- 环境：macOS、Apple M4、Godot 4.6.3 stable、Compatibility/OpenGL 4.1 Metal；逻辑 1280×720，`--qa-test` 隔离玩家存档。无界面编辑器导入、既有 UI 回归通过，无解析或资源错误；UI 快速退出仍有此前记录的 ObjectDB 警告。
- 临时 OpenGL 检查脚本实际渲染准备、暂停悬停、失败提示与重开画面：整条提示行无文字、圆点和跳过入口；单位详情含当前生命与战斗数据，错误提示可读。验证暂停时 3 秒错误提示仍会消失、成功重开后无旧提示。截图、脚本和日志保存在 `/tmp`，不纳入仓库。
- 原生临时 QA 窗口真实鼠标检查：关闭小铺、开战、选布丁放置第三行第二格、暂停后尝试放置、Esc 取消和悬停单位均正常；暂停失败操作未新增单位或扣热量，浮动详情显示生命 130/130。默认及放大窗口观察到原提示行无残留。本轮未完整复验八关、性能或重新导出发行包。
- 工作区已有并持续发生操作栏、小铺、顶部外观及棋盘几何调整，均予以保留；本节仅验收提示栏与引导移除，不替代其他调整的验收记录。


## 上下贴边与游戏区扩展（2026-09-13）

状态：已实现并完成本次布局及交互验收。范围为「使用体验」的顶部贴边下延、底部靠边及棋盘扩展；设计与当前几何以 [棋盘布局](../art/perspective.md) 为准。

- 环境：macOS、Apple M4、Godot 4.6.3 stable、Compatibility/OpenGL；逻辑 1280×720。使用已有美术展示场景及 `/tmp/FVMEdgeQA.app` 临时官方 Godot 副本，通过独立应用标识定位窗口，`--qa-test` 隔离存档；临时包装和日志不入库。
- Godot 无界面编辑器导入通过；`python3 tools/run_tests.py` 九组通过。既有 projection 用例更新为新画布边界及 76 像素正方形，315 个格内采样、外侧拒绝与行内路径检查通过；UI 回归覆盖费用、重复放置、暂停、取消、八卡与浮层输入。既有音频 ObjectDB 退出警告仍按前文记录。
- 真实窗口检查顶部面板从游戏画布上边框向下延伸，八张卡及锅铲完整；底部按钮、取消提示、关卡及进度完整。棋盘扩大后边框、行号、入口、角色及血条对齐；右侧三张食谱与棋盘不重叠。
- 鼠标将第一行第一格爆米花移至第七行第九格，悬停生命保持 150/150；底部开始后选布丁并在第七行第一格放置。暂停后点击第七行第二格没有新增单位，热量保持 115，Esc 清除选卡。验证使用展示场景测试阵容，不作为正常资源策略或关卡平衡验收。
- 通过系统窗口缩放检查约 640×392 与 1224×768 的捕获画面（含标题栏及系统捕获缩放）：正方形及宽高比保持，卡牌、底部 UI 和棋盘完整，非 16:9 区域保留黑边。顶部贴边指游戏画布边框，不覆盖系统标题栏或等比缩放黑边。
- 保留同期小铺弹窗及提示栏移除改动；未验证完整八关真人、其他设备 DPI 或扩大后性能，未重新导出安装包，不据此标记完整使用体验里程碑完成。


## 紧凑顶栏与整卡冷却（2026-09-13）

状态：已实现并完成本次验收。范围：移除红框状态文字及锅铲文字，紧凑卡牌、整卡冷却与棋盘再次扩大。设计依据 [UI 规范](../art/ui.md#紧凑顶栏与整卡冷却2026-09-13) 和 [棋盘布局](../art/perspective.md)。保留本任务开始时已有的 UI、小铺、提示栏及布局改动。

- 环境：macOS、Apple M4、Godot 4.6.3 stable、Compatibility/OpenGL 4.1 Metal，逻辑 1280×720；使用 `--qa-test` 隔离存档、既有美术展示场景，以及 `/tmp/FVMCompactQA.app` 的本机官方 Godot 临时副本。无新增依赖或外部资产；新增原创 `scripts/ui/card_cooldown.gdshader` 与 UID。
- 无界面编辑器导入通过，无新增解析及引用错误；`python3 tools/run_tests.py` 九组 PASS。UI 回归更新覆盖整卡遮罩边界、鼠标忽略、独立材质、满／半／零比例、暂停冻结、卡牌无状态节点、锅铲无文字与八卡高度；原有费用、重复放置、暂停拒绝、取消、铲除确认及小铺测试通过。投影回归通过 315 个格内采样、外侧拒绝、82×82 正方形与连续行路径。
- 实际 OpenGL 渲染 `/tmp/fvm-compact-cooldowns.png` 检查独立卡片剩余比例 1、0.5、0.25、0：阴影覆盖全部图文，顶部先露出，半冷却只覆盖下半卡，结束无阴影；圆角无外溢，选中描边可辨。此图使用受控状态检查遮罩，不作为正常阵容策略验证。
- 独立真实窗口鼠标将第一行第一格小笼包移至第七行第九格，悬停生命仍为 180/180；开战后布丁放在第一行第一格，卡片呈整卡冷却。暂停时点第七行第一格未放置、未扣热量；Esc 取消，切换 2×并恢复后冷却最终清空，再暂停正常。
- 实际观察默认及系统放大窗口（捕获约 640×392、1224×768，含标题栏）：资源、八卡与纯图标锅铲完整，顶栏更薄，棋盘上下边框及底栏完整，等比缩放仍保持方格和黑边。已有食谱面板几何与棋盘不重叠，UI 自动边界用例通过；本轮未重复三选一鼠标操作。
- 未重新导出发行包，未验证完整八关真人、其他设备 DPI 或扩大后性能；不据此标记完整使用体验／平衡里程碑通过。临时应用、渲染图与日志不入库。

## 透明锅铲按钮与光标（2026-09-13）

状态：已实现；导入、九组回归、真实窗口按钮／交互／缩放检查通过。设计依据 [UI 规范](../art/ui.md#透明锅铲按钮与光标2026-09-13)，属于「使用体验」局部调整。

- 右上角改为 60×60 透明方形点击区，图标居中，正常／悬停／激活／禁用均无底色；悬停和激活以图标着色反馈。原有原创 SVG 同时生成 32×32 原生小锅铲光标，不新增外部素材或依赖。
- 环境：macOS、Apple M4、Godot 4.6.3、Compatibility/OpenGL 4.1 Metal，逻辑 1280×720。无界面编辑器导入通过，`python3 tools/run_tests.py` 九组 PASS；新增光标状态用例覆盖单击、重复切换、选卡互斥、暂停及恢复、确认弹窗及关闭、取消、小铺、离开可操作阶段与激活时退出场景。原有重复放置、费用、移除确认与交易等回归通过；快速退出仍有既有 ObjectDB 警告。
- 使用 `/tmp/FVMCompactQA.app` 临时官方 Godot 副本启动主场景，`--qa-test` 隔离玩家存档。真实窗口鼠标关闭小铺、激活锅铲、点击空格显示「格子为空」；开战并放置吐司后，激活锅铲点击吐司弹出确认，取消后吐司保留；暂停尝试移除被拒绝，Esc 取消。默认约 640×392 与系统放大约 1224×768 的捕获画面中，图标居中且无按钮背景，顶栏和其他控件完整。
- 捕获工具在鼠标位置叠加自身指针，无法用本轮窗口截图直接复核原生自定义光标的最终外观／DPI；原生光标注册、32×32 纹理及激活／恢复状态已接入并通过测试，仍需直接目视复核系统光标外观。准备移除沿用原逻辑，本轮未重复真实窗口移除；未重导发行包或执行完整真人／性能验收。
- 工作期间出现同期卡牌拖放改动，已保留，并将本项切卡用例改为真实输入信号以兼容当前卡牌入口；本节不替代拖放功能验收。临时应用和日志不入库。

## 顶部热量图标（2026-09-13）

状态：已实现。范围为「使用体验」的左上角热量呈现，依据 [UI 规范](../art/ui.md#顶部热量显示2026-09-13)。新增原创火焰 SVG 及导入配置，数量移至图标右侧，移除原「热量」文字；保留工作区其他进行中的卡牌与交互改动。

- macOS、Apple M4、Godot 4.6.3、Compatibility/OpenGL；使用 `--qa-test` 隔离存档。无界面编辑器导入和既有 `tests/test_ui.gd` 通过，无解析、引用或断言错误。
- 实际 OpenGL 渲染检查 000、150、350，图标与数量清晰、无重叠，粮仓和金币仍位于下一行。默认运行窗口（捕获 640×392，含标题栏）准备阶段观察到火焰左、150 右，战斗画面数值刷新后图标仍正常。
- 本轮窗口验证期间其他界面改动仍在进行，未将暂停或系统放大窗口操作记为独立完成的人工复验；既有 UI 回归含暂停与取消。未重复完整业务、真人、性能或发行导出。临时截图、脚本与日志位于 `/tmp`，不入库。
