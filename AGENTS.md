# 项目协作与代理规范

本规范适用于《深夜食堂：鼠潮来袭》（`food-vs-mouse`）的所有代码、场景、资源、文档和交付工作。将本仓库按正式 Godot 项目维护，不以原型阶段为由省略设计记录、模块边界或必要验证。

## 1. 开始工作前

1. 阅读 [文档索引](doc/README.md)、[首版设计与实施计划](doc/TODO.md) 和 [工程实践](doc/engineering.md)。
2. 阅读任务涉及模块的设计、决策及验收记录，检查仓库当前状态和用户已有改动。
3. 明确本次需求对应的里程碑、修改范围和验收方式，再开始实现。
4. 不覆盖、回退或清理与当前任务无关的用户改动；不将计划中的功能描述为已实现。

## 2. 文档是设计依据

- **所有设定和设计都必须以文档形式落实在 `doc/`。** 包括玩法、数值、角色、关卡、剧情、美术、音频、交互、技术架构、数据接口、存档和发布方案。对话、代码注释、提交说明和资源文件不能代替设计文档。
- `doc/TODO.md` 是首版范围、玩法及里程碑的基线。新增设计不得静默改变其中的约束。用户明确要求变更时，同步更新基线和相关文档。
- 实现新行为前，先补齐足以指导实现的设计：规则、输入输出、状态变化、边界情况、数据和验收标准。小改动可更新已有文档，无须每次另建文件。
- 存在歧义时，检查已有文档；涉及玩法、范围或不可逆接口的关键缺口向用户澄清。可逆的常规实现选择自行决定并记录。未确认内容标为“待定”，不得伪装成既定规则。
- 同一规则只维护一个权威说明，其他文档通过链接引用。发现冲突时明确记录并解决，不凭代码现状反向认定设计。
- 文档与实现必须在同一交付中更新；已实现、拟议、待验证和已废弃必须明确区分。

## 3. 工程约束

- 技术目标沿用 `doc/TODO.md`：Godot 4.6、GDScript、2D、桌面端、鼠标操作、Compatibility、1280×720 等比缩放。不得擅自切换引擎版本、语言或渲染方案。
- 遵循 [工程实践](doc/engineering.md) 的目录、代码、场景、数据、存档和验证约定。工程规范新增或变更也必须记录在 `doc/`。
- 游戏配置使用自定义 `Resource` 和稳定字符串 ID；运行状态独立保存，不修改共享定义资源，不将平衡数值散落在脚本中。
- 保持 `RunController`、`BoardController`、`CombatController`、`WaveDirector`、`ShopController`、`RecipeSystem`、`SaveService` 的职责边界。UI 发出操作请求并展示结果，业务层校验合法性。
- 对跨模块事件使用明确信号或接口；伤害集中结算，计时统一遵循游戏时间。禁止通过任意节点路径或全局变量隐式耦合模块。
- 不擅自增加首版排除的联机、抽牌、分支地图、装备词条、无尽模式和移动端适配；不顺手扩大重构范围。
- 引入插件、运行依赖、外部资产或导出要求时，在 `doc/` 记录用途、版本、来源和适用许可。遵循原创内容要求。

## 4. 工作与交付流程

1. **设计**：更新相关 `doc/` 文档，明确本次验收条件及未决问题。
2. **实现**：以最小完整功能推进，保持场景、资源和脚本引用有效，处理异常及重复操作。
3. **验证**：根据风险完成相关逻辑验证、Godot 导入检查和可视交互验收；不能用无界面运行代替视觉验证。
4. **记录**：在 `doc/` 写明影响较大的功能或缺陷验证结果、运行环境和剩余问题，更新文档索引及实际完成状态。
5. **交付**：说明改动、关联文档、实际执行的验证及限制。未运行、无法运行或尚未通过的检查如实标注。

## 5. 完成标准

- 行为符合已记录的设计，相关设定、接口和边界在 `doc/` 可查。
- 相关场景可加载、脚本无新增解析错误、资源无丢失引用；涉及交互时已完成相应人工验证。
- 本次改动涉及的 `doc/TODO.md` 必验项已验证，或明确说明未验证原因；不得仅因代码完成就标记里程碑通过。
- 不提交 `.godot/`、本机存档、日志、密钥、临时产物或导出包；保留应纳入版本控制的源资源及 UID 文件。
- 文档链接有效，没有将目标配置、估算性能或未来能力写成已验证事实。
