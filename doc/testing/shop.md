# 商店统一食谱与灵感验收

日期：2026-09-13。状态：已实现，导入、13 组回归及窗口操作通过；经济平衡待试玩。环境：macOS、Apple M4、Godot 4.6.3、Compatibility，当前工作区。依据 [商店设计](../design/shop.md) 与 [首版基线](../TODO.md)。

- 执行 Godot `--headless --path . --editor --import --quit`，无新增解析或资源错误。
- 执行 `tests/test_*.gd` 共 13 组：animation、art、builds、content、core、drag、edges、heat、heat_ui、progression、projection、save、ui 全部 PASS。UI 原选谱阶段取消测试改为胜利阶段取消后复跑通过；其余相关新增断言另行复跑通过。
- builds 覆盖美食与食谱同种子结果、食谱余额不足、战斗阶段拒绝、扣费与重复请求、空池；progression 覆盖三关连续结算、无免费食谱、奖励不重复；save 覆盖购物往返、旧 recipe 快照迁移、重复恢复不抽新商品／重发奖励；UI 覆盖同店购买、扣费、商品移除、弹窗边界与开闭。
- 使用已有 `/tmp/FVMShopPopupQA.app` 本机官方 Godot 临时包装器运行隔离 art_gallery（`--qa-test`），真实鼠标购买高压蒸笼：金币 99→95，已购食谱 00→01，商品移除；再次点击原位置金币不变。完成购物关闭，重新打开保持购买状态，Esc 关闭后点击开战正常进入战斗。
- 实际默认约 640×392 与放大约 1224×768 窗口捕获（含标题栏／黑边）检查，上排美食、下排食谱、金币、价格和底部操作无裁切或重叠。该夹具使用受控阵容和金币，仅验证 UI，不代表正常通关。
- `git diff --check`、文档相对链接检查通过。快速 UI／拖放／拾取测试退出仍有此前记录的 ObjectDB 警告；没有新增断言或脚本错误。临时包装器、日志和游戏存档不入库。

限制：本轮没有重导发行包，没有验证新价格下的八关经济与两种构筑通关，也未重复真人／性能验收。原免费食谱规则下的平衡报告属于历史记录，不能视为当前付费规则的验收结论。
